api_version = "2026-01"
uid = "208e9edf-8b9b-6b74-3f3a-539fdede2328f859a084"

[[extensions]]
name = "Tangbuy Bundle Discount"
handle = "bundle-discount"
type = "function"
description = "Applies kit / combo / mix (% or flat) / free-gift discounts from tangbuy_* metafields"

  [[extensions.targeting]]
  target = "cart.lines.discounts.generate.run"
  input_query = "src/cart_lines_discounts_generate_run.graphql"
  export = "cart-lines-discounts-generate-run"

  [[extensions.targeting]]
  target = "cart.delivery-options.discounts.generate.run"
  input_query = "src/cart_delivery_options_discounts_generate_run.graphql"
  export = "cart-delivery-options-discounts-generate-run"

  # Empty command → CLI built-in JS builder (esbuild + javy).
  [extensions.build]
  command = ""
  typegen_command = "true"
  path = "dist/function.wasm"
  watch = [ "src/**/*.js", "src/**/*.graphql" ]
